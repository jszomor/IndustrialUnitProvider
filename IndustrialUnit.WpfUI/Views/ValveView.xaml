<UserControl x:Class="IndustrialUnit.WpfUI.Views.ValveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:IndustrialUnit.WpfUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="849" d:DesignWidth="1457">

  <UserControl.DataContext>
    <viewModel:ValveViewModel/>
  </UserControl.DataContext>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Grid.RowSpan="5">
      <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
        <Label Content="Id" Grid.Row="0"/>
        <TextBox Text="{Binding Id}"  Grid.Row="1"/>
        <Label Content="Item type" Grid.Row="2"/>
        <TextBox Text="{Binding ItemType}" Grid.Row="3"/>
      </StackPanel>
    </DockPanel>
  </Grid>

</UserControl>
